<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="severity" value="warning"/>
    <property name="fileExtensions" value="java"/>
    <property name="charset" value="UTF-8"/>

    <!-- EditorConfig: end_of_line = lf -->
    <module name="RegexpMultiline">
        <property name="format" value="\r\n"/>
        <property name="message" value="[newline-lf] Line must end with LF, not CRLF"/>
    </module>

    <!-- EditorConfig: insert_final_newline = true -->
    <module name="NewlineAtEndOfFile">
        <property name="lineSeparator" value="lf"/>
    </module>

    <!-- EditorConfig: trim_trailing_whitespace = true -->
    <module name="RegexpSingleline">
        <property name="format" value="^(?!\s+\* $).*?\s+$"/>
        <property name="message" value="[no-trailing-spaces] Line has trailing spaces."/>
    </module>

    <!-- EditorConfig: max_line_length = 120 / Checkstyle: 최대 라인 길이 120자 -->
    <property name="tabWidth" value="4"/>
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
        <message key="maxLineLen"
                 value="[line-length-120] Line is longer than {0,number,integer} characters (found {1,number,integer})"/>
    </module>

    <module name="TreeWalker">
        <!-- 패키지명: 소문자 연속 사용 -->
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
            <message key="name.invalidPattern"
                     value="[package-lowercase] Package name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 클래스명: PascalCase -->
        <module name="TypeName">
            <message key="name.invalidPattern"
                     value="[class-pascalcase] Type name ''{0}'' must match pattern ''{1}''."/>
        </module>

        <!-- 변수명 및 메서드명: camelCase -->
        <module name="MethodName">
            <property name="format" value="^[a-z][a-z0-9][a-zA-Z0-9_]*$"/>
        </module>
        <module name="MemberName">
            <property name="format" value="^[a-z][a-zA-Z0-9][a-zA-Z0-9]*$"/>
        </module>
        <module name="ParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9][a-zA-Z0-9]*$"/>
        </module>
        <module name="LocalVariableName">
            <property name="tokens" value="VARIABLE_DEF"/>
            <property name="format" value="^[a-z][a-zA-Z0-9][a-zA-Z0-9]*$"/>
            <property name="allowOneCharVarInForLoop" value="true"/>
        </module>

        <!-- 상수명: UPPER_SNAKE_CASE -->
        <module name="ConstantName"/>

        <!-- 들여쓰기 크기 4 (EditorConfig indent_size와 동일, 백스톱용) -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="lineWrappingIndentation" value="4"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!-- 제어문 블록 {}: K&R 스타일 (선언부와 같은 줄에 { 시작) -->
        <module name="LeftCurly"/>
        <module name="RightCurly">
            <property name="option" value="alone"/>
            <property name="tokens" value="CLASS_DEF, METHOD_DEF, CTOR_DEF, LITERAL_FOR, LITERAL_WHILE, STATIC_INIT, INSTANCE_INIT"/>
        </module>
        <module name="RightCurly">
            <property name="option" value="same"/>
            <property name="tokens" value="LITERAL_TRY, LITERAL_CATCH, LITERAL_FINALLY, LITERAL_IF, LITERAL_ELSE, LITERAL_DO"/>
        </module>
        <module name="NeedBraces"/>

        <!-- 연산자 배치: 연산자 앞뒤 공백 -->
        <module name="WhitespaceAround">
            <property name="tokens"
                      value="ASSIGN, BAND, BAND_ASSIGN, BOR, BOR_ASSIGN, BSR, BSR_ASSIGN, BXOR, BXOR_ASSIGN, DIV,
                             DIV_ASSIGN, EQUAL, GE, GT, LAND, LE, LOR, LT, MINUS, MINUS_ASSIGN, MOD, MOD_ASSIGN,
                             NOT_EQUAL, PLUS, PLUS_ASSIGN, QUESTION, SL, SL_ASSIGN, SR, SR_ASSIGN, STAR, STAR_ASSIGN,
                             TYPE_EXTENSION_AND"/>
        </module>

        <!-- import 순서: java -> javax -> jakarta -> org -> com, wildcard 금지 -->
        <module name="AvoidStarImport">
            <property name="allowStaticMemberImports" value="true"/>
            <message key="import.avoidStar"
                     value="[avoid-star-import] Using the '.*' form of import should be avoided - {0}."/>
        </module>
        <module name="ImportOrder">
            <property name="groups"
                      value="java, javax, jakarta, org, com, /^(?!java\.|javax\.|jakarta\.|org\.|com\.).*/"/>
            <property name="ordered" value="true"/>
            <property name="separated" value="true"/>
            <property name="option" value="top"/>
            <property name="sortStaticImportsAlphabetically" value="true"/>
            <message key="import.groups.separated.internally"
                     value="[import-grouping] Extra separation in import group before ''{0}''"/>
            <message key="import.ordering"
                     value="[import-grouping] Wrong order for ''{0}'' import."/>
            <message key="import.separation"
                     value="[import-grouping] ''{0}'' should be separated from previous imports."/>
        </module>

        <!-- 줄 바꿈: 클래스, 메서드 선언 및 로직 단락 구분 시 빈 줄 1줄 -->
        <module name="EmptyLineSeparator">
            <property name="tokens" value="METHOD_DEF"/>
        </module>

        <!-- 위키에 없는 규칙에 예외가 필요할 때 인라인으로 끌 수 있는 suppression -->
        <module name="SuppressionCommentFilter">
            <property name="offCommentFormat" value="@checkstyle:off"/>
            <property name="onCommentFormat" value="@checkstyle:on"/>
        </module>
    </module>

    <!--- Exclude module-info.java -->
    <module name="BeforeExecutionExclusionFileFilter">
        <property name="fileNamePattern" value="module\-info\.java$"/>
    </module>
</module>
